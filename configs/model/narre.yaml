# @package _global_

model_name: narre

model:
  hidden_dim: 32
  num_filters: 100
  kernel_size: 3
  dropout: 0.3

training:
  batch: 32
  eval_batch: 256
  epoch: 600
  lr: 0.001
  weight_decay: 1e-4
  lr_decay: 0.99  
  optimizer: Adam