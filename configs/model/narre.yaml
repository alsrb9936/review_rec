# @package _global_

model_name: narre

model:
  freeze_word_embedding: false
  hidden_dim: 32
  num_filters: 100
  kernel_size: 3
  dropout: 0.6

training:
  batch: 64
  eval_batch: 256
  epoch: 600
  lr: 0.001
  weight_decay: 2e-3
  lr_decay: 0.99  
  optimizer: Adam
