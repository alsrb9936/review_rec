# @package _global_

model_name: cfarg

model:
  variant: gated
  result_name: CFARG-gate-control
  embedding_dim: 64
  gate_hidden_dim: 64
  dropout: 0.1
  review_scale_init: 0.1
  review_scale_trainable: false
  gate_bias_init: -2.0
  gate_reg_weight: 0.001
  detach_cf_for_gate: true
  use_align_gate: true
  align_temperature: 5.0
  align_threshold: 0.0

data:
  type: bert
  exclude_target_review: true

training:
  lr: 0.001
  batch: 256
  eval_batch: 512
  epoch: 100
  weight_decay: 1e-4
  lr_decay: 0.99
  optimizer: Adam
  grad_clip: 1.0

evaluation:
  eval_step: 1
  early_stop_patience: 10
  gate_sample_size: 1000

experiment:
  results_dir: ./results/cfarg_gate_control
  result_file: ./results/cfarg_gate_control/main_results.csv
  fast_dev_run: false
  fast_dev_samples: 64
  debug: false

noise:
  enabled: false
  type: random_review_replacement
  ratio: 0.0
  side: both
