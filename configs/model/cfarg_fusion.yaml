# @package _global_

defaults:
  - cfarg
  - _self_

model:
  variant: fusion
  result_name: Whole-review residual fusion
  score_mode: residual
  review_scale_init: 0.003
  review_scale_trainable: false
  use_align_gate: false
  gate_reg_weight: 0.0
