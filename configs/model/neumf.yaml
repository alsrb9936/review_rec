# @package _global_
# NeuMF model config - overrides default config values

model_name: neumf
hidden_dim: 64
num_layers: 3
dropout: 0.2

data:
  load_review_text: false

training:
  lr: 0.0005
  batch: 512
  epoch: 150
