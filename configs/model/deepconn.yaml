# @package _global_

model_name: deepconn
  
model:
  freeze_word_embedding: false
  hidden_dim: 50
  num_filters: 100
  kernel_size: 3
  dropout: 0.5

training:
  batch: 64
  eval_batch: 128
  epoch: 600
  lr: 0.002
  weight_decay: 1e-3
  lr_decay: 0.99  
  optimizer: Adam